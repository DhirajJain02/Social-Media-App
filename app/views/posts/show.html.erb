<div class="container mx-auto p-6">
  <h1 class="text-2xl font-bold mb-4"><%= @post.title %></h1>
  <p class="text-lg mb-4"><%= @post.description %></p>

  <%= link_to 'Edit', edit_post_path(@post), class: "bg-yellow-500 text-white px-4 py-2 rounded-lg" %>
  <%= link_to 'Back', posts_path, class: "bg-gray-500 text-white px-4 py-2 rounded-lg" %>
</div>
